# SIiIW Lista 4 — zależności
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
ucimlrepo>=0.0.7      # pobieranie zbioru UCI id=878 (opcjonalne, można użyć lokalnego CSV)
tabulate>=0.9         # ładne tabele -> markdown do raportu
